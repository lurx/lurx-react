/** Delay in ms between starting each download (avoids browser blocking). */
export const DOWNLOAD_DELAY_BETWEEN_MS = 500;

/** Extra ms to wait after last download before running cleanup. */
export const DOWNLOAD_CLEANUP_EXTRA_MS = 1000;
